# 다루마!! 설정 가이드

처음부터 다시 설치하거나, 다른 선생님 PC/계정으로 옮길 때 참고하는 단계별 안내입니다.

---

## 1. 구글 스프레드시트 + Apps Script 연결

1. 결과를 저장할 구글 스프레드시트를 새로 만듭니다.
2. 메뉴에서 **확장 프로그램 > Apps Script**를 클릭합니다.
3. 기본으로 열려 있는 코드를 모두 지우고, [AppsScript_Code.gs](AppsScript_Code.gs) 파일 내용 전체를 붙여넣습니다.
4. 저장(디스크 아이콘) 후, 우측 상단 **[배포] > [새 배포]**를 클릭합니다.
5. 유형 선택에서 톱니바퀴를 눌러 **"웹 앱"**을 선택합니다.
6. **실행 계정**은 "나(내 계정)", **액세스 권한이 있는 사용자**는 "모든 사용자"로 설정합니다.
7. **[배포]**를 누르고 권한 승인을 진행합니다. ("Google에서 확인하지 않은 앱" 경고가 뜨면 "고급" > "(프로젝트 이름)으로 이동"을 눌러 진행 — 본인이 만든 스크립트이므로 안전합니다.)
8. 배포 완료 후 나오는 **웹 앱 URL**을 복사합니다.
9. [index.html](index.html)에서 `CONFIG.APPS_SCRIPT_URL` 값을 그 URL로 바꿉니다.

> **주의**: `AppsScript_Code.gs` 코드를 수정한 뒤에는 반드시 **[배포] > [배포 관리] > 연필 아이콘 > 새 버전**으로 다시 배포해야 변경 사항이 실제 웹 앱에 반영됩니다. URL 자체는 바뀌지 않습니다.

처음 배포 후 사이트에 한 번 접속하면 스프레드시트에 "문제은행"·"한자읽기" 탭이 자동으로 생성되고 기본 문제/사전으로 채워집니다.

---

## 2. GitHub 저장소로 코드 관리

이 프로젝트는 GitHub 저장소 **[hooyou0429/daruma2026](https://github.com/hooyou0429/daruma2026)** (`main` 브랜치)로 버전 관리됩니다.

- 로컬 작업 폴더에서 파일을 수정한 뒤, 변경 사항을 커밋하고 `origin/main`으로 push합니다.
- 이 저장소에는 세션 마무리 시 변경 내용을 자동으로 커밋·push하는 `/done` 스킬(`.claude/skills/done/SKILL.md`)이 설정되어 있어, Claude에게 "저장해줘" / "여기까지 올려줘"라고 말하면 알아서 커밋 메시지를 작성하고 push합니다.
- 수동으로 하려면:
  ```bash
  git add -A
  git commit -m "변경 내용 요약"
  git push origin main
  ```

---

## 3. Netlify 배포 (GitHub 연동 자동 배포)

**과거 방식**: 완성된 파일을 Netlify 사이트에 드래그앤드롭으로 직접 업로드.

**현재 방식**: Netlify 사이트가 위 GitHub 저장소와 연결되어 있어, **`main` 브랜치에 git push하면 Netlify가 자동으로 감지해 새로 배포**합니다. 별도로 파일을 업로드할 필요가 없습니다.

- 이 프로젝트는 정적 HTML/JS 파일이라 별도의 빌드 명령이 필요 없고, publish directory는 저장소 루트(`/`)입니다.
- 배포 진행 상황과 로그는 Netlify 대시보드의 **Deploys** 탭에서 확인할 수 있습니다. push 후 보통 1분 이내에 반영됩니다.
- 저장소 연결이 끊어졌거나 새 Netlify 사이트를 처음 만드는 경우: Netlify 대시보드 > **Add new site > Import an existing project > GitHub** > `daruma2026` 저장소 선택 > 브랜치 `main` > 빌드 명령 없음 / publish directory `.` 로 설정하면 됩니다.

### Netlify 비공개(로그인 요구) 문제

2026년 7월 말부터 Netlify의 새 프로젝트가 기본적으로 **비공개(Private)**로 생성되는 정책이 생겼습니다. 학생들이 접속할 때 로그인/비밀번호를 요구한다면:

**Project configuration > General > Visitor access > Project visibility > Public** 으로 전환하세요.

---

## 4. 문제(연습 문장) / 한자 채점 보정 사전 관리

파일을 건드릴 필요 없이 **구글 스프레드시트**에서 바로 관리합니다.

- **연습 문장 추가/수정/삭제**: 시트의 **"문제은행"** 탭에서 카테고리 / 일본어 문장 / 한국어 뜻 세 칸을 편집. 후리가나가 필요하면 일본어 문장 칸에 `<ruby>漢字<rt>よみ</rt></ruby>` 형식으로 입력합니다.
- **한자→히라가나 채점 보정 추가**: 시트의 **"한자읽기"** 탭에 한자(또는 인식될 수 있는 표기) / 히라가나(정답으로 처리) 두 칸으로 한 줄 추가.
- 저장하면 학생이 다음에 접속할 때 바로 반영되며, `index.html`/`questions.js` 수정이나 재배포가 필요 없습니다.
- 인터넷 문제 등으로 시트를 못 불러올 때는 `questions.js`(문제)와 `index.html`에 내장된 기본 사전(한자읽기)으로 자동 대체되어 학생 화면이 멈추지 않습니다.

---

## 5. 빠르게 바꾸고 싶을 때 어디를 보면 되는지

자세한 표는 [README.md](README.md)의 "8. 참고 — 무엇을 바꾸려면 어디를 봐야 하는지" 절을 참고하세요.
